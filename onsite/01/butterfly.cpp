#include <bits/stdc++.h>

using namespace std;

int main() {

  char character;
  cin >> character;

  int butterflyCount;
  cin >> butterflyCount;

  for (int i = 0; i < n / 2; i++) {
    for (int j = 0; i <= i; j++ ) {
      cout << ch;
    }

    for (int j = i; j < n / 2; j++) {
      cout << '.';
    }

    cout << endl;
  }

  return 0;
}
