#include <bits/stdc++.h>

using namespace std;

int main() {
    int count;
    cin >> count;

    stringstream output;

    for (int i = 0; i < count; i++) {
        int n;
        cin >> n;



        pair<string, vector<int, int>> bruh;

        for (int j = 0; j < n; j++) {
            char cmd;
            cin >> cmd;

            if (cmd == 'a') {
                int x, y;
                string s;

                cin >> s;
                cin >> x;
                cin >> y;

                                                                                
            }
        }
    }

    return 0;
}